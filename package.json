{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "build:check": "node build.mjs --check"
    },
    "dependencies": {
        "choices.js": "10.2.0"
    },
    "devDependencies": {
        "esbuild": "^0.28.1",
        "prettier": "3.9.4"
    }
}
